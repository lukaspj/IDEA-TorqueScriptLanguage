<ModuleDefinition
        canSave="true"
        canSaveDynamicFields="true"
        ModuleId="${NAME}"
        VersionId="1"
        Group="Game"
        scriptFile="${NAME}"
        CreateFunction="onCreate"
        DestroyFunction="onDestroy">
    <DeclaredAssets
            canSave="true"
            canSaveDynamicFields="true"
            Extension="asset.taml"
            Recurse="true" />
</ModuleDefinition>
